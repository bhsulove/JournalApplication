
spring:
  data:
    :mongodb:
      uri: ${MONGO_URI}
      database: journaldb
      auto-index-creation: true

server:
  port: 8080
  servlet:
    context-path: /journal

weather:
  api:
    key: ${WEATHER_KEY}





